import React from 'react';


const Slider = () => {

    return (
            <div id="slider" className="slider-big">
            <h1>Cada obra de arte puede tener múltiples interpretaciones y significados. Es importante recordar que no existe una única respuesta correcta en el arte. Cada persona puede tener su propia interpretación y eso enriquece la experiencia.</h1>
        </div>
    );
}

export default Slider;